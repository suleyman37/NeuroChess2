# A0 Local Stack Config

Detect Codex CLI, Ollama, text critic model, and vision critic model. Write the
availability report to the external autopilot artifact root. Do not dirty the
repo for runtime state.
