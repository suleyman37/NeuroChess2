# A0 AgentOS Bootstrap

Create or maintain the repo-native AgentOS files only. Do not touch product
frontend/backend code. Run docs/tooling checks and promote only if the diff is
inside the bootstrap allowlist.
